<apex:page controller="Calculator">
    <apex:sectionHeader title="Scientific Calci" subtitle="Mera Calculator" />
    <apex:form >
    	<apex:pageBlock title="Scientific Calculator" >
        	<apex:pageBlockButtons location="bottom" >
                <apex:commandButton value="Addition" action="{!addition}" reRender="res" />
                <apex:commandButton value="Subtract" action="{!subtraction}" reRender="res" />
                <apex:commandButton value="Multiply" action="{!multiplication}" reRender="res" />
                <apex:commandButton value="Divide" action="{!division}" reRender="res" />
                <apex:commandButton value="Factorial" action="{!factorial}" reRender="res" />
                <apex:commandButton value="Power" action="{!power}" reRender="res" />
                <apex:commandButton value="Sin" action="{!sin}" reRender="res" />
                <apex:commandButton value="Max" action="{!maximum}" reRender="res" />
            </apex:pageBlockButtons>
			<apex:pageBlockSection title="Calci">
                <apex:inputText label="Enter first number" html-placeholder="first number" value="{!firstNum}" />
                <apex:inputText label="Enter second number" html-placeholder="second number" value="{!secondNum}" />
                <apex:outputText label="Result" value="{!result}" id="res" />
            </apex:pageBlockSection>
        </apex:pageBlock>
    </apex:form>
</apex:page>
                 